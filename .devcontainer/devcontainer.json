{
	"name": "Existing Dockerfile",
	"build": {
		"context": "..",
		"dockerfile": "../Dockerfile"
	},
	"features": {},
	
	// This will run npm install when the container starts
	"postCreateCommand": "npm install"
}